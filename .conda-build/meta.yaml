{% set version = "2.2.0" %}
{% set py_version = "3.10" %}
{% set cuda_version = "cuda12.1" %}
{% set cublas_version = "12.1.3.1" %}
{% set cusparse_version = "12.1.0.106" %}
{% set cudart_version = "12.1.105" %}
{% set numpy_version = "1.24.3" %}
{% set mkl_version = "2023.1.0" %}
{% set arch_pip = "unix" %}

package:
  name: magtense
  version: {{ version }}

build:
  number: 0
  string: py{{ py_version }}_{{ cuda_version }}
  script_env:
   - MT_VERSION={{ version }}
   - CUDA_PIP={{ cuda_version }}
   - PY_PIP={{ py_version }}
   - ARCH_PIP={{ arch_pip }}

requirements:
  host:
    - python {{ py_version }}
    - pip
  run:
    - python {{ py_version }}
    - numpy =={{ numpy_version }}
    - matplotlib
    - mkl =={{ mkl_version }}
    - intel-fortran-rt =={{ mkl_version }} # [unix]
    - libcusparse =={{ cusparse_version }} # [unix]
    - libcusparse-dev =={{ cusparse_version }} # [win]
    - libcublas =={{ cublas_version }} # [unix]
    - libcublas-dev =={{ cublas_version }} # [win]
    - cuda-cudart =={{ cudart_version }} # [unix]
    - cuda-cudart-dev =={{ cudart_version }} # [win]

test:
  imports:
    - magtense
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://www.magtense.org/
  summary: MagTense - a micromagnetism and magnetostatic framework
  dev_url: https://github.com/cmt-dtu-energy/MagTense
  doc_url: https://cmt-dtu-energy.github.io/MagTense/
  license: GPL-3.0-or-later
  license_file: ../LICENSE

extra:
  recipe-maintainers:
    - spol@dtu.dk
